/* exported setValue */

/*
 Define a function named gsetValue that accepts three argunemts, `object`, `key`, and `value`
    set value for an object's key and property
*/

function setValue(object, key, value) {
  object[key] = value;
}
