/* exported getValue */

/*
  Define a function named getValue that accepts two argunemts, `object` and key
    return the value for an object's property key
*/

function getValue(object, key) {
  return object[key];
}
