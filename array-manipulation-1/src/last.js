/* exported last */

/*
  Define a function named last that accepts one argument, `array`
    return value of the last index of the array
*/

function last(array) {
  return array[array.length - 1];
}
